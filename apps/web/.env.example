# Public — safe in the browser bundle.
NEXT_PUBLIC_SUPABASE_URL=https://kdjghlybcecvzowxzsqz.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only. Comma-separated list of emails allowed to sign in.
# An empty value locks everyone out rather than letting everyone in.
OWNER_EMAIL=

# NOTE: SUPABASE_SERVICE_ROLE_KEY is deliberately absent. The web app has no
# need for it, and adding it would put a full-access credential one
# misconfiguration away from the browser.
